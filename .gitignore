# Local-only documents
docs/internal/

# Go
/avero
# go.work and go.work.sum are committed: the gate needs the workspace, which
# joins the host module and the CLI module.
*.exe
*.test
*.out

# Build and verification output
/artifacts/
/dist/
/tmp/

# Environment
.env
.env.local

# Editor and OS
.DS_Store
.idea/
.vscode/
