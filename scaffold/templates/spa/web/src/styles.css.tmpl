@import "tailwindcss";

body {
    margin: 0;
    font-family: system-ui, sans-serif;
    line-height: 1.5;
}

main {
    max-width: 40rem;
    margin: 0 auto;
    padding: 1rem;
}

.done label {
    text-decoration: line-through;
}

.error {
    color: #b91c1c;
}
